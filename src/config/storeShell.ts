/**
 * Largura da vitrine: use o par inset + content em sequência.
 * O teto horizontal vem de `--store-content-max` em `src/app/globals.css` (classe `max-w-store`).
 */
export const storeShellInset = "px-6 sm:px-10 lg:px-16 xl:px-20";
export const storeShellContent = "mx-auto w-full max-w-store";
